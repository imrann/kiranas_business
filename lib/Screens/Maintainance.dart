import 'package:flutter/material.dart';

class Maintainance extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text("This Page is under Maintainance")),
    );
  }
}
